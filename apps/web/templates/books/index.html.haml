%h2 Books list
#books
  - if books.any?
    %table
      %thead
        %th title
        %th author
        %th actions
      %tbody
        - books.each do |book|
          %tr
            %td
              = book.title
            %td
              (
              = book.author
              )
            %td
              = link_to 'Edit', routes.edit_book_path(book.id)
              = link_to 'Delete', '/'
  - else
    .no-books There are no books yet
